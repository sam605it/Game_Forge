import { createStubTemplate } from "./stub";

export const cityBuilder = createStubTemplate({
  id: "city_builder",
  label: "City Builder",
  category: "simulation",
  worldMode: "grid2d",
  controls: "click_place",
});
