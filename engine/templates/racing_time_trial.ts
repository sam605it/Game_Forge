import { createStubTemplate } from "./stub";

export const racingTimeTrial = createStubTemplate({
  id: "racing_time_trial",
  label: "Racing Time Trial",
  category: "racing",
  worldMode: "topdown2d",
});
